---
# Golang role defaults
# Set to a version string (e.g. "1.26.0") to pin, or "latest" to install latest stable
go_version: "latest"

# Hardcoded fallback when both uri and curl fail to reach go.dev
go_version_fallback: "1.26.0"
