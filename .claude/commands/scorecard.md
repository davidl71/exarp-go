---
description: Run exarp-go project scorecard (fast mode)
---

Call the exarp-go `report` MCP tool with `action=scorecard`, `fast_mode=true`, `compact=true`, `output_format=json`.

Summarize the scorecard results: overall score, per-dimension scores, blockers, and recommended next actions.
